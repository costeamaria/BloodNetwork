@page
@model BloodNetwork.Pages.Appointments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Programări</h1>

<div class="text-black">
    <a asp-page="Create" class="btn btn-outline-danger">
        <i class="bi bi-plus-circle">Fă o nouă programare</i>
    </a>
</div>
<div class="row">
    <div class="col-md ">

        <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].Member)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].Member.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].Clinic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].AppointmentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].AppointmentTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment[0].Clinic.Doctor)
            </th>
            <th>Acțiuni</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Appointment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Member.FullName)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Member.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clinic.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppointmentDate)
            <td>
                @Html.DisplayFor(modelItem => item.AppointmentTime)
            </td>
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Clinic.Doctor.DoctorName)
                </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary" asp-page="./Edit" asp-route-id="@item.ID"><i class="bi bi-pencil-square"></i>Edit</a> |
                            <a class="btn btn-outline-info" asp-page="./Details" asp-route-id="@item.ID"><i class="bi bi-eye"></i>Details</a> |
                            <a class="btn btn-danger" asp-page="./Delete" asp-route-id="@item.ID"><i class="bi bi-pencil-square"></i>Delete</a>
                        </td>
        </tr>
}
    </tbody>
</table>
</div>
</div>
